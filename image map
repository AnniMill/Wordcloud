import numpy as np
from PIL import Image

mask_file = st.file_uploader("🔺 Upload Image Mask (black background, transparent shape)", type=["png", "jpg"])

mask_array = None
if mask_file:
    image = Image.open(mask_file).convert("L")  # Convert to grayscale
    mask_array = np.array(image)
